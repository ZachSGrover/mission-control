# ── Mission Control — Production Requirements ─────────────────────────────────
# Generated from uv.lock (pyproject.toml) — all versions are exact pins.
# Python 3.12+ required (matches Render runtime).
# Dev-only packages (black, ruff, mypy, pytest, flake8, etc.) are excluded.
#
# Render build command:  pip install -r requirements.txt
# Render start command:  uvicorn app.main:app --host 0.0.0.0 --port $PORT
# ──────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.131.0
fastapi-pagination==0.15.10
starlette==0.52.1
uvicorn==0.40.0
uvloop==0.22.1           # fast event loop — Linux only (Render is Linux)
httptools==0.7.1         # uvicorn performance parser
watchfiles==1.1.1        # uvicorn standard dep
sse-starlette==3.2.0     # Server-Sent Events streaming
websockets==16.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.46
sqlmodel==0.0.32
alembic==1.18.3
greenlet==3.3.1          # required for sqlalchemy asyncio
psycopg==3.3.2           # async PostgreSQL driver
psycopg-binary==3.3.2    # pre-built binary (no libpq-dev needed on Render)
mako==1.3.10             # alembic template engine

# ── Auth / Clerk ─────────────────────────────────────────────────────────────
clerk-backend-api==4.2.0
pyjwt==2.11.0
librt==0.7.8             # clerk-backend-api dep
pytokens==0.4.1          # clerk-backend-api dep
docstring-parser==0.17.0 # clerk-backend-api dep

# ── AI providers ──────────────────────────────────────────────────────────────
openai==2.31.0
anthropic==0.93.0
google-genai==1.71.0
google-auth==2.49.1
pyasn1==0.6.3            # google-auth dep
pyasn1-modules==0.4.2    # google-auth dep

# ── Background jobs (RQ / Redis) ──────────────────────────────────────────────
rq==2.6.0
redis==6.3.0
croniter==6.0.0          # rq scheduler dep
python-dateutil==2.9.0.post0
pytz==2025.2

# ── Settings / config ─────────────────────────────────────────────────────────
pydantic==2.12.5
pydantic-core==2.41.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# ── Encryption ────────────────────────────────────────────────────────────────
cryptography==45.0.7
cffi==2.0.0              # cryptography C binding

# ── HTTP / networking ─────────────────────────────────────────────────────────
httpx==0.28.1            # used directly in auth.py + clerk dep
httpcore==1.0.9
h11==0.16.0
requests==2.33.1
urllib3==2.6.3
certifi==2026.1.4
charset-normalizer==3.4.7
idna==3.11

# ── Templates ────────────────────────────────────────────────────────────────
jinja2==3.1.6
markupsafe==3.0.3

# ── Shared utilities ──────────────────────────────────────────────────────────
annotated-types==0.7.0
annotated-doc==0.0.4
anyio==4.12.1
click==8.3.1
distro==1.9.0
jiter==0.13.0
packaging==26.0
pyyaml==6.0.3
six==1.17.0
sniffio==1.3.1
tenacity==9.1.4
tqdm==4.67.3
typing-extensions==4.15.0
typing-inspection==0.4.2
tzdata==2025.3
